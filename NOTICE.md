This repository vendors a small subset of `@electron/asar` under the MIT license.

Vendored files live under:

- `lib/asar_vendor`

Included upstream license copy:

- `lib/asar_vendor/LICENSE.md`

Those files are used only to extract, repack, and hash `app.asar` without requiring a separate npm install step.
